# T-REX dependency (PLANNED)

Install Tokeny T-REX before using `script/deploy/DeployTREX.s.sol`:

```bash
forge install TokenySolutions/T-REX --no-commit
```

Add remappings in `foundry.toml` per Tokeny docs, then implement `DeployTREX.run()`.

Spec: [`../../_docs/contracts-erc3643.md`](../../_docs/contracts-erc3643.md) §2.
